#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int _putchar(char c);
int _pow_recursion(int x, int y);
unsigned int binary_to_uint(const char *b);

#endif /* MAIN_H */
